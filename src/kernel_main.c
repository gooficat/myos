
#include "types.h"

static u16 *VGA = (u16 *)0xB8000;

void kernel_main(void)
{
  // char *hello = "Hello, World";
  // for (int i = 0; hello[i]; ++i)
  // {
  //   VGA[i] = 0b00000111 << 8 | hello[i];
  // }
}
